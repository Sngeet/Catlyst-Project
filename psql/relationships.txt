relationships in PostgreSQL/SQL, they describe how tables are connected to each other.

1. One-to-One (1:1)

One record in Table A is related to one record in Table B.

Example:

Person
------
id
name

Passport
--------
id
passport_number
person_id

One person → one passport.

2. One-to-Many (1:N)

One record in Table A can be related to many records in Table B.

This is the most common relationship.

Example:

Department
----------
id
name

Employee
--------
id
name
department_id

One department → many employees.

Department 1 ───────< Employee

In SQL, this is usually implemented using a Foreign Key:

CREATE TABLE department (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100)
);

CREATE TABLE employee (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    department_id INT REFERENCES department(id)
);

3. Many-to-Many (M:N):-

Many records in Table A can relate to many records in Table B.

Example:

Student
-------
id
name

Course
------
id
name

A student can take many courses, and a course can have many students.

We create a junction/intermediate table:

Student
   |
   | 
Student_Course
   |
   |
Course
CREATE TABLE student (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100)
);

CREATE TABLE course (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100)
);

CREATE TABLE student_course (
    student_id INT REFERENCES student(id),
    course_id INT REFERENCES course(id),
    PRIMARY KEY (student_id, course_id)
);